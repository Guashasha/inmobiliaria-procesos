import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class PropertyCUsTest {
    @Test
    fun buscarPropiedadesDeZona () {
        assert(true)
    }

    @Test
    fun buscarPropiedadesDeZonaVacía () {
        assert(true)
    }
}